import React from 'react';

const LoadingSpinner: React.FC = () => (
  <div className="h-screen w-full flex items-center justify-center font-bold text-teal">
    Loading TalentOS...
  </div>
);

export default LoadingSpinner;
